# Azure AI Foundry / Azure OpenAI Default Configuration Settings (Optional overrides)
# Dynamic values entered in Settings UI dashboard are stored client-side in browser local storage.
# Define fallback defaults below on the server-side if needed:

NEXT_PUBLIC_DEFAULT_AZURE_ENDPOINT=""
NEXT_PUBLIC_DEFAULT_AZURE_API_KEY=""
NEXT_PUBLIC_DEFAULT_AZURE_DEPLOYMENT_NAME=""
NEXT_PUBLIC_DEFAULT_AZURE_API_VERSION="2024-02-15-preview"
NEXT_PUBLIC_DEFAULT_AZURE_MODEL_NAME="gpt-4o"
